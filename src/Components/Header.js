import React from 'react';
import Nav from "./Nav";
import "./Components_CSS/Header.css"

const Header = () => {
    return (
        <header>
            LOGO
            <Nav />
        </header>
    );
};

export default Header;